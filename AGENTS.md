# 工作约定

<!-- 唯一正文。CLAUDE.md 只有一行 @AGENTS.md 引到这里。改规则只改这个文件。 -->
<!-- 这里只放「模型有能力但不会稳定去做」且「结果可验证」的事。 -->
<!-- 风格规范归 ruff,机械可检测的症状归 scripts/check.py,都不要写进来。 -->

## 禁令

**代码结构**

- 禁止在代码里对自由文本(用户 query / 对话消息 / 模型输出)做关键词或正则判断来猜测意图。
  这类判断归 prompt、工具描述、输出 schema。
  豁免:协议识别、安全过滤/脱敏、显式命令解析(`/help` 这种)、以及对自己定义的枚举值
  (状态码、节点 type、配置 key)的判断 —— 这些不是在猜意图。
  迁到 prompt 后同等受审:prompt 里写「如果用户说 X 就 Y」等价于代码里的特例补丁。
- 生产逻辑里不许硬编码具体用户输入字符串、具体 ID。
  `tests/`、fixture、复现脚本、日志、文档全部豁免 —— 精确回归测试正需要这些。
- 删除旧实现前必须确认无调用方。grep 不够:还要查动态导入、装饰器注册、配置字符串引用、
  CLI 入口、反射/序列化。确认不了就问,不要默认可删。
  确认无调用方就直接删,不留 `_v2` / `_legacy` / shim。不接受「保留旧的以防万一」。
- 不实现我没要求的功能。

**错误处理(不变量)**

- 失败不得被转换成看似成功的结果。
  判据是「调用方能否分辨这是真结果还是失败」,不是具体形态 —— mock、固定返回、假数据、
  空字符串、捕获异常后的降级值、占位 DTO、默认字典都算。
  豁免:`tests/`;重试 / 熔断 / 批处理里的显式失败处理不算,只要失败对调用方可见。

**卡住时**

- 同一问题连续 3 次修复失败,停止试新变体,报告卡点和需要我提供什么。不要自行回滚。

## 动手前

- 用第三方库 API 前先确认**已安装版本**的实际签名(`inspect.signature`、类型声明、
  对应版本的文档)。不凭记忆,也不直接信在线最新文档 —— 可能与 lock 的版本不符。
- 修 bug 前先给出因果链:哪一层抛的、上游调用链、为什么产生观察到的症状。
  定位不到具体行是允许的(并发、时序、配置组合常常如此),明说「未定位到行,当前假设是 X」,
  不许编一个过度确定的因果故事。
- **先说清这个失败输入属于哪一类,修复覆盖整类,不是这一条。**
  说不出类别就是还没找到根因,先别写代码。
  (类别也是下面「验证」那条造变体的依据 —— 不知道类,就造不出真正的同类变体。)
- 新写一个函数前,说明现有的相近实现为何不可复用,或查符号引用 / 同层接口 / 注册表确认没有。

## 验证

- **修 bug 时,除原始失败输入外,再准备 2-3 个同类变体和 1-2 个邻近反例(相似但不该触发的输入),
  一起验证。** 这是本文件里最重要的一条 —— 其他检查只能看代码外形,只有这条能证明修复泛化。
- 没实际执行过命令,不许说「已验证」。
- 汇报必须带范围:跑了什么、覆盖了什么、什么没验证及原因。「跑过命令」不等于「验证过功能」。
- 要改测试断言,必须用需求、协议、历史行为或其他独立证据证明**原断言写错了**。
- 改完跑 `python scripts/check.py --diff`。输出全是警告、允许误报;
  确认是误报就在那行加 `# check: ignore[规则名] 理由`,不要为了让它闭嘴而改代码结构。

## 沟通

- 默认简体中文。代码注释、commit message 用英文。
- 先给结论,再给理由。不堆术语,也不要解释我已经懂的基础概念。
- 汇报只写三件事:改了什么、验证到哪一步(带范围)、哪里需要我决定。
  不复述过程、不总结刚做完的操作。
- 给建议要给出推荐方案和原因,不只罗列选项。
- 不确定的地方直接标出来,不要含糊带过。做了我没要求的事,必须说。
- 按风险分档:
  - 低风险 → 直接做,做完一句话说明。
  - 先说再做,满足任一条即可:**不可逆**(删文件、改 DB schema);
    **有外部副作用**(发消息、产生费用、暴露数据 —— 可回滚也算);
    **影响面广**(改公共接口签名、改已验证过的 prompt、装新依赖)。

<!-- 项目事实(技术栈、端口、启动命令、目录结构)写在下面或单独的 docs/,不要和上面的规则混在一起。 -->
