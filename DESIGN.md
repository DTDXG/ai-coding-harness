# AI Coding 规则体系设计 — 讨论存档

> **这是推导过程的存档,不是当前状态。当前状态看 [README.md](README.md)。**
> 保留它是因为 README 只说结论,而「为什么是这几条、哪些方案被否掉了、为什么否」
> 只在这里 —— 缺了它,后来人(包括几个月后的自己)会把已经推翻的结论捡回来重试一遍。
>
> 状态:短任务方案已完成一轮**外部批判性复审并大幅修订**(2026-07-31),
> 并已产出可运行的第一版实现(即本仓库)。
> ~~**下一步是跑 `experiments/falsification.md` 的证伪实验,而不是把规则铺进项目。**~~
> ⚠️ **2026-08-03 改变**:改为**先铺进项目,等下一个真实的单 case 故障就地做实验**。
> 理由是实验第一步自己写的「选一个真实故障(不要造)」—— 预先跑就必须造样本,
> 而造出来的例子会照着规则的形状造,变成自证。触发条件见该文档顶部。
> **代价:在此之前规则处于「没被证伪过」而非「验证过」的状态。**
> 长任务方案留待后续单独讨论。
>
> **⚠️ 阅读顺序建议:先读第十二节(复审记录),再读正文。**
> 正文中带 ⚠️ 标记的段落都是被推翻或下修过的结论 —— 保留原文是为了记录推导过程,
> 但**不要把已划掉的结论捡回来**。
>
> 公开前做过脱敏:内部仓库名替换为「仓库 A / B / C」,生产文件路径改为示意路径,
> 实测数字和代码形态原样保留 —— 数据是这份文档的价值所在。

---

## 一、背景与目标

用户是 Python 开发者,主要做 LLM / agent 系统(Agno、多 agent 编排、多模态生成)。

**问题诊断**:现有几份 CLAUDE.md(三个生产仓库,下称仓库 A / B / C)写的都是"项目是什么",缺的是"你该怎么干活"。而实际踩的坑全属于后者。

**额外约束**:
- 方案必须在 **Claude Code 和 Codex 上都能用**
- 不能靠"让 AI 多说话"来解决 —— 要**对用户沟通友好**,不是更啰嗦
- hook / skill / 脚本等能力都可以用

---

## 二、用户反馈的实际问题

1. 针对单个 case 优化,不找根因
2. 习惯加规则匹配(而项目是 agent 系统,不是老的规则体系)
3. 每次加功能只增不删,单文件越来越大
4. 重复代码
5. 单 agent 持续做事会陷入局部,无法纠正或发现其他问题
6. 见到问题就做,不思考、不分析、不搜索
7. 任务过程中不知道利用本地 skill(codex-bash、code-simplifier 等)

补充的同源问题:声称验证过但没跑、改测试断言让测试通过、LLM 调不通就写假返回。

---

## 三、四条设计启发式(非公理)

> ⚠️ **2026-07-31 下修**:初版称这四条是"整个方案的地基,所有结论由它们推出"。
> 经 codex 批判性复审,它们**只是有用的设计偏好,不是可推导的公理** ——
> 每条都有反例,且文档自身多处违反。详见第十二节。以下为修订版。

### 1. 删掉这行,会变差吗?(原:模型会犯错吗)

**问题**:原版是二元判断,而模型行为是概率性的。一条规则可能只把失败率从 8% 降到 5%,
删掉不必然犯错,但仍有价值。原版还完全忽略**后果**。

**修订**:快速判断保留(必须能秒答,否则没法用),但加一个后果维度 ——
> 删掉会变差吗?**低频但高危**的(泄密、删数据、静默错误)即使几乎不触发也保留。

据此仍可删:目录树、函数签名、ruff 已强制的格式规范、"请编写高质量代码"。

> codex 提出改用"预期损失 > 总成本"。理论正确但**不可操作** ——
> 你没法给一条 markdown 规则估触发频率 × 失败概率变化 × 影响。故不采纳。

### 2. 三分法:有能力 / 会不会稳定调用 / 可不可验证(原:倾向 vs 能力)

**问题**:原版"能力 vs 倾向"是伪二分,而且我用一个反例论证了自己 ——
原文说"先 grep 属于能力,模型已会,故过期",但痛点 #6 恰恰是**"会搜索却不搜索"**,
那正是不稳定调用。同样,"查文档""读完 traceback""定位代码行"模型全都有能力,
规则的目的是强制它**实际去做**。

**修订**为三个维度:
1. 模型**有没有**这个能力 → 没有,写规则也没用
2. 在当前工具/上下文/奖励结构下,**会不会稳定调用** → 不稳定 = 规则的主战场
3. 调用结果**可不可观察、可不可验证** → 不可验证的规则等于没有

**保留的内核**:针对模型已能稳定做好的事写规则 = 死重量,会堆积并稀释活着的规则。
**删掉的部分**:"固有倾向不会过期"没有依据 —— 调优、工具策略、产品行为都可能改变它。

### 3. 避免仪式性输出,优先要求可验证证据(原:减法优先于加法)

**问题**:原版把"无额外文字"等同于"零成本",混淆了**沟通成本**和**系统成本**:
- 禁止关键词判断 → 可能多一次 LLM 调用(延迟、费用、不确定性)
- 禁止异常降级 → 可能让整个批任务失败
- 禁令越多,越容易出现改名、抽 helper、移配置这类**合规规避**

**修订**:真正的分界不是加法/减法,而是 ——
> 要求产生**对任务无价值的可见仪式**,还是**可验证的工程证据**?

正向动作也可以零仪式:改完跑受影响的测试、新增回归测试时覆盖邻近负例。
反过来,禁令也可能引出大段辩解文字。

| | 例子 | 判断 |
|---|---|---|
| 可验证证据 | 跑测试、给出变体验证结果 | **好** —— 即使是"加法" |
| 只改动作不产文字 | "3 次失败就停" | 好 |
| 无价值的可见仪式 | "每次改动前写三段分析"、"至少换 3 种说法搜索" | **差** —— 会被当作业应付 |

### 4. 硬约束用于不变量,软偏好用于真权衡(原:缩范围不降强度)

**问题**:原版说"软化措辞 = 全局失效",但文档自己用了"优先改现有文件""默认简体中文"
"建议运行 code-simplifier"。而且"范围内 100% 生效"对 LLM 指令不成立 ——
服从始终是概率性的;"生产路径""降级值""影响面广""同一问题"也都不可机械判定。

**修订**:
- **可判定的不变量** → 硬约束 + 缩范围(不降强度)。原判据在这里成立。
  实例:"失败不得伪装成成功"、"没跑不许说验证过"
- **真正的工程权衡** → 用软偏好,但**必须写出权衡的两端**,不能只说"尽量"。
  反例:"尽量不要硬编码"(无效);正例:"拆文件 vs 保持内聚,超过 800 行时倾向拆"

**仍然成立的部分**:误伤的第一解法是缩范围,不是软化措辞。
"不许 mock 绕过失败调用"不豁免 `tests/` 就会误伤全部单测,然后被整条删掉。

### 附:优秀规则的检查表(非必要条件)

原版称这三条是"必要条件",过强 —— "公开 API 必须向后兼容"是重要约束却不绑定时机动作;
而本文档自己的多条规则也不可二元判定("无谓新建文件""影响面广""同一问题")。

作为**检查表**仍然有用:
1. **可判定** —— 越接近 yes/no 越好
2. **绑定动作** —— 指定时机和具体操作
3. **给替代路径** —— 只说"别做 X",模型会换个姿势接着做 X

---

## 四、跨平台方案

### 配置文件单一来源
- `AGENTS.md` —— 唯一正文(跨工具事实标准)
- `CLAUDE.md` —— 一行 `@AGENTS.md`,用 Claude Code 的 import 语法引过去

这样不论 Claude Code 是否原生支持 AGENTS.md,都只有一份内容要维护。

### 规则放仓库,不放平台配置
```
第一层  规则本身   →  scripts/check.py(纯 Python,零平台依赖)
第二层  自动触发   →  平台 hook 只负责调用该脚本(薄触发器,一行)
                      Claude Code: PostToolUse / Stop hook
                      Codex: hook 能力未核实,退路是写进 AGENTS.md 工作流
第三层  最终兜底   →  pre-commit + CI 跑同一个脚本
```
第三层是最强的一道,但**不是绝对保险**(原文写"不会漏"是过度承诺):
`pre-commit` 可被 `--no-verify` 跳过;CI 只拦得住**检查器认识的模式**。
它无法证明模型没有改坏测试、没有伪造验证结论、没有把特例移进 prompt。

> ~~⚠️ 待核实:Codex CLI 当前的 hook 能力(讨论时搜索工具不可用,未凭记忆断言)。~~
> ✅ **2026-08-03 已核实(codex-cli 0.146.0,查实际二进制 + 实跑)**:Codex **有 hooks,stable 且默认启用**。
> 事件名与 Claude Code 完全一致(`PreToolUse` / `PostToolUse` / `Stop` / `SubagentStart` /
> `SubagentStop` / `SessionStart` / `SessionEnd` / `UserPromptSubmit` / `PreCompact` /
> `PostCompact` / `PermissionRequest`),契约也一致(`exit 2` + stderr、`decision: block` + reason)。
> 配置文件是 `hooks.json`。**本仓库尚未提供 Codex 侧 `hooks.json`,这是现在真实存在的缺口。**
> 另外 Codex 也有 subagent:`collaboration.spawn_agent`(需 `features.multi_agent = true`)。

---

## 五、短任务方案:四层结构

> ⚠️ **2026-07-31 补第四层**。初版只有三层,全部在检查「代码长什么样」,
> 没有一层检查「修复是否泛化」—— 而"只修一个 case"恰恰是泛化问题,不是外形问题。
> 这是原架构最大的结构性缺陷,详见第十二节。

| 层 | 作用 | 常驻成本 |
|---|---|---|
| `AGENTS.md` 禁令 + 沟通约束 | 约束不稳定调用与偏好 | **常驻 → 必须 ≤80 行** |
| **行为验证(新增,最重要)** | **证明修复泛化:变体 + 邻近反例** | 零(写进 AGENTS.md 验证段) |
| skill(code-simplifier 等) | 需判断但不必常驻的清单 | 零(只有一行 description) |
| `check.py` + ruff + hook / pre-commit | 机械可检测的症状,尽力兜底 | 零 |

**关键性质:后三层常驻成本为零。** 真正每轮付费的只有 AGENTS.md 那 80 行。
这正面回应了"不要啰嗦"的诉求。

**推论**:AGENTS.md 里每多写一条能被脚本查、能被 skill 承载的东西,
都是拿常驻预算换本可零成本的东西。所以它必须极度克制。

**但注意**:第四层(机械检查)只能抓**症状的语法形态**,抓不到根因。
一份实现可以 ruff 全绿、check.py 全绿、skill 不触发,而只修好了那一个输入。
唯一能证伪这一点的是第二层。

---

## 六、AGENTS.md 内容(≤80 行,待定稿)

### 禁令

> ⚠️ **2026-07-31 大幅修订**。初版多条规则有副作用、内部冗余或自相矛盾,
> 已按 codex 复审意见重写。逐条原因见第十二节。

**代码结构**
- 禁止在代码里对**自由文本**(用户 query / 对话消息 / 模型输出)做关键词或正则判断
  来**猜测意图**;这类判断归 prompt、工具描述、输出 schema。
  **豁免**:协议识别、安全过滤/脱敏、显式命令解析(`/help` 这种)、
  以及对自己定义的枚举值(状态码、节点 type、配置 key)的判断 —— 这些不是在猜意图。
  ⚠️ **迁到 prompt 后必须同等受审** —— 见第七节 D,否则只是把特例挪进了盲区。
- **生产逻辑**里不允许硬编码具体用户输入字符串、具体 ID。
  **`tests/`、fixture、复现脚本、日志、文档全部豁免** —— 精确回归测试正需要这些。
- 删除旧实现前必须确认无调用方。**grep 不够** —— 还要查动态导入、装饰器注册、
  配置字符串引用、CLI 入口、反射/序列化(你的 Agno 工具注册就是这类)。
  确认不了就问,不要默认可删。
  确认无调用方 → 直接删,不留 `_v2` / `_legacy` / shim。**禁止「保留旧的以防万一」**。
- 不实现我没要求的功能。

> 已删除原「优先改现有文件,避免新建文件」—— 与痛点 #3(单文件膨胀)直接冲突,
> 等于奖励往 God file 里继续堆。建不建文件由**职责边界**决定,不由文件数量决定。

**错误处理(原三条合并为一条不变量)**
- **失败不得被转换成看似成功的结果。**
  涵盖但不限于:mock / 固定返回绕过外部调用、LLM 失败返回假数据或空字符串、
  捕获异常后返回降级值、占位 DTO、默认字典。
  **判据是「调用方能否分辨这是真结果还是失败」**,不是具体形态 ——
  否则换成缓存对象或 helper 函数就绕过了。
  豁免:`tests/`;重试 / 熔断 / 批处理里的显式失败处理不算,**只要失败对调用方可见**。

**卡住时**
- 同一问题连续 3 次修复失败后,停止尝试新变体,报告卡点和所需信息。
  (原版写"回滚到干净状态" —— 已删,可能毁掉未提交改动。回不回滚由我决定。)

**动手前**
- 用第三方库 API 前先确认**已安装版本的实际签名**(`inspect.signature`、类型声明、
  对应版本的文档),不凭记忆,**也不直接信在线最新文档** —— 可能与 lock 的版本不符。
- 修 bug 前先给出因果链:哪一层抛的、上游调用链、为什么产生观察到的症状。
  **定位不到具体行是允许的**(并发、时序、配置组合问题常常如此)——
  但要明说"未能定位到行,当前假设是 X",**不许编一个过度确定的因果故事**。

> 已删除原「至少换 3 种说法搜索」—— 典型的仪式性合规:只保证执行三次字符串检索,
> 不保证找到语义重复(重复实现可能用完全不同的领域命名)。
> 改为要求:说明候选实现**为何不可复用**,或查符号引用/同层接口/注册表。

**验证(本轮强化,是防单 case 优化的主力)**
- **修 bug 时,除原始失败输入外,再准备 2-3 个同类变体和 1-2 个邻近反例
  (相似但不该触发的输入),一起验证。**
  这比任何静态检查都有效 —— 静态检查只能看代码外形,这个才能证明修复泛化。
- 没实际执行过命令,不许说「已验证」。
- **汇报必须带范围**:跑了什么、覆盖了什么、什么没验证及原因。
  "跑过命令"不等于"验证过功能"。
- 要修改测试断言,必须用需求、协议、历史行为或其他独立证据证明**原断言写错了**。
  (原版"测试失败默认是代码错"已删 —— 会形成错误锚定,测试也可能过时或非确定。)

### 沟通约束

- 默认简体中文。(待补:代码注释 / commit message 用什么语言)
- 先给结论,再给理由。不为显得严谨而堆术语,也不要解释我已经懂的基础概念。
- 汇报只写三件事:改了什么、验证到哪一步(带范围)、哪里需要我决定。
  不复述过程、不总结刚做完的操作、不邀功。
- 给建议要给出推荐方案和原因,不只罗列选项。
- 不确定的地方直接标出来,不要含糊带过。做了我没要求的事,必须说。
- **按风险分档**(原版只看可逆性,不充分):
  - 低风险 → 直接做,做完一句话说明
  - **先说再做**,满足任一条:
    - 不可逆(删文件、改数据库 schema)
    - **有外部副作用**(发消息、产生费用、暴露数据)—— 可回滚也算高风险
    - 影响面广(改公共接口签名、改已验证过的 prompt、装新依赖)

---

## 七、机械检查层的分工

**别重复造 ruff。**

| 检查对象 | 工具 | 管什么 |
|---|---|---|
| Python 语言规范 | `ruff` A 组 | 现成规则,替代手写风格指南 |
| 复杂度与残留 | `ruff` B 组 | **仅作审查触发器**,不是根因证据(见下) |
| 剩余 AI 反模式 | `check.py` | ruff 覆盖不到的,**全部为警告** |
| **prompt / schema** | **人工 review + 契约测试** | **原设计的盲区,见 D** |
| 功能级判断 | skill | 需要理解语义的 |

### ⚠️ 关于"模型已经能 cover 这些规范了,还要 ruff 吗?"

**要,但理由要说准。** 第三节那些启发式是为 `AGENTS.md` 设计的 ——
那里每行**每轮对话都付费**,所以必须严筛。
ruff 是一次性配置、毫秒级运行、**零上下文成本**(但**不是零总成本**,见配置策略一节)。

**ruff 的约束对象不是模型,是仓库**:历史代码(几年前的人 / 老模型写的)、
多来源(你 / Codex / Claude / 同事)、以及长上下文后期质量下滑的生成结果。

### 归 ruff — A. 语言规范(仓库 B 那份 140 行风格规范九成可由此替代)

| 规范 | ruff 规则 |
|---|---|
| 命名(snake_case / PascalCase) | `N` |
| 行宽 120 / 双引号 / 缩进 | `ruff format` |
| 禁止 `from x import *` | `F403` |
| 禁止可变默认参数 | `B006` |
| 禁止空 except / bare except | `E722` + `S110` |
| 禁止 `print()` | `T201` |
| 类型注解 | `ANN` |
| 导入分组排序 | `I` |
| 禁止硬编码密钥 | `S105` / `S106` |
| Google docstring | `D` + convention |

只有"循环内不做 DB/网络请求""不用中文变量名"需自己写。

### 归 ruff — B. 复杂度与残留检测(2026-07-31 补,**同日下修**)

> ⚠️ **原表述已被实测证伪**。初版称这几条是"单 case 优化最可靠的机械代理指标"
> 且"零误判" —— **两句都不成立**。以下为下修后的定位。

| ruff 规则 | 实际能抓的 | 定位 |
|---|---|---|
| `ERA001` | 注释掉的死代码 | **有效** —— 直接对应"只增不删" |
| `F401` / `ARG` | 未使用的 import / 参数 | **有效** —— 改动残留痕迹 |
| `C901` / `PLR0912` / `PLR0915` | 复杂度 / 分支数超阈值 | **仅作审查触发器**。对单个特例召回≈0;对合法的协议解析器、状态机会误报 |
| `PLR2004` | 比较中的**数值**魔法值 | 弱。提成命名常量即消失,且完全不管字符串 |
| ~~`SIM` 系列~~ | —— | ⚠️ **`SIM116` 必须 ignore** —— 见下 |

**实测(codex,本机 ruff 0.4.4)**:

| 样本 | 结果 |
|---|---|
| `if "refund" in query: return refund_agent` —— 真正的单 case 补丁 | **零报警** |
| 字典里 30 个硬编码 case | **零报警** |
| 合法的 13 分支协议解析器 | **触发 C901 + PLR0912 + SIM116** |

即:**该抓的抓不到(单个特例只加 1 个分支,离阈值 10 差得远),不该抓的抓了。**

**`SIM116` 尤其要关掉**:它建议把连续 `if` 改写成字典 ——
而字典恰恰是特例最难被检测的藏身处。**这条规则在主动帮模型把问题藏起来。**

模型还可以通过这些方式让指标变绿而问题不变:每个特例抽成小 helper、
特例移进 JSON/YAML、移进 prompt、字面量提成命名常量、用注册表代替 if/elif。

**真正能防单 case 优化的是第六节「验证」里那条变体 + 邻近反例要求,不是静态指标。**
这些 ruff 规则只能作为复杂度提醒和人工审查触发器,不能作为"修了根因"的证据。

### 配置策略:分批开 + 固定版本(原「广开别精挑」下修)

原推理"合规代码不报警所以零噪音"是**循环论证** —— "合规"正是由这些规则定义的。
零上下文成本 ≠ 零总成本:`noqa` 债务、CI 摩擦、框架回调 / fixture / 动态代码误报、
版本升级后的规则漂移,都是真成本。

但也不回到精挑(精挑的结果就是漏掉了整个 B 表)。折中 —— **分批开**:

```toml
[tool.ruff]
required-version = "0.x.y"    # 必须固定:规则行为会随版本漂移

[tool.ruff.lint]
# 第一批(低争议,先开)
select = ["E","F","W","I","B","UP","T20","ERA","ARG","RUF"]
# 第二批(第一批跑顺了再加):"N","S","ANN","D","C90","PLR","SIM"
ignore = ["SIM116"]           # 理由见上表
```

**存量仓库**:一次性全开会炸出几千条历史违规。
先 `ruff check --fix` 自动修一轮 → `--statistics` 看分布 →
暂时不动的进 `ignore` 或用 baseline 圈住,只对新代码生效。

### 归 check.py(**全部降为警告 + 需理由豁免**)

> ⚠️ 原设计有「零误报拦截项」,已证伪 —— 三条都有直接反例:
> - 800 行文件里新增函数,**很可能正是在拆分巨型函数** —— 原规则会拦住正确行为
> - 生产路径里的 fake 可能是明确的 simulator / dry-run / 开发适配器
> - `_v2` 可能是真实的外部 API 版本号,`new_client` 也可能是正常变量名
>
> 而且全部可绕过:改名 `_next` / `_replacement`、把 fallback 移进 helper 或配置文件。
> 反向漏网也明显:在 799 行文件里给原函数加 200 行、不新增函数只加分支,都不会被拦。

```
【全部为警告,不拦截,允许误报】
- 单文件 > 800 行
- 生产代码里疑似 mock / 固定返回绕过外部调用(tests/ 豁免)
- 新增 _v2 / _new / _legacy 命名
- 对自由文本做关键词匹配
  (AST:对字符串字面量做 in / == 比较,且变量名含
   query / message / prompt / text / content / response)
- 疑似重复函数(函数体相似度比对)
- LLM 调用失败后 fallback 到空字符串 / 假数据
```

**豁免机制(原设计缺失)**:每处豁免写一行理由注释,`check.py` 统计豁免次数。
某条规则长期只被豁免 → 说明规则错了,删掉。

**轻量命中日志**:每次把命中的规则名追加到 `.check-hits.log`。一个月后看:
从来没命中的 → 删;总被豁免的 → 改;误报率高的 → 收窄。
十行代码,不需要企业级治理体系。

### D. prompt / schema 也要检查(⚠️ 2026-07-31 晚,前提被推翻并重写)

> ⚠️ **原表述错了**。初版说"禁止在代码里判断自由文本 → 特例迁进 prompt 这个检查盲区",
> 把「迁到 prompt」整体判定为逃逸。用户指出这是错的,我认同。原文保留在下方对照。

**真正的判据不是「代码 vs prompt」,是这个机制会不会泛化。**

`if "退款" in query` 坏,不是因为"系统里存在一个特例",而是因为**字符串匹配没有泛化能力** ——
"我想把钱要回来""这个我不要了能退吗""refund pls" 全部漏掉,它只认那一个写法。

而 prompt 里写「如果用户想退款,调用 refund 工具」,执行者是 LLM,上面那些变体都覆盖得到。
**这不是把特例藏起来,这就是修好了。** 向量 / 语义召回同理。

| 形式 | 会不会泛化 | 判断 |
|---|---|---|
| 代码里字符串匹配 | ❌ | 坏 |
| prompt 里描述**意图类别** | ✅ | **好,这就是该有的样子** |
| 向量 / 语义召回 | ✅ | 好 |
| prompt 里**逐条粘用户原话** | ❌ | 坏 —— 同一个病换了载体 |

只有最后一行是问题:

```
如果用户说"我要退款" → refund
如果用户说"这个东西我不想要了" → refund
如果用户说"能退钱吗" → refund
```

这不是在描述概念,是在给模型一张字符串查找表。它的来源和代码里那个特例完全一样 ——
线上报一个 case 就粘一行进去。只增不删,prompt 无限膨胀,最后自己跟自己冲突。

**所以规则的范围要收窄到「引号里的具体原话」,不能匹配「如果用户说」这个句式本身** ——
后者会把「如果用户说想退款就调 refund」这种完全正常的 prompt 也报出来,典型误伤。
判据:引号里是**自然语言短语** = 用户原话;是**标识符 / 枚举值**(`"imageNode"`、`"error"`)
= schema 引用,不算。已按此重写 `check.py` 的 `prompt_case_patch_hit`,10 条对照用例全过。

仍然成立的部分:

- prompt / 工具描述 / schema 文件纳入 diff review,不允许"顺手改一下"
- structured output 用 schema 约束并做**契约测试**,不要正则解析模型输出
- prompt 改动同样适用第六节的变体 + 邻近反例验证

> **原文(已废,保留供对照)**:
> ```
> 代码里的硬编码规则  →(被禁止)→  prompt 里的硬编码规则
>                                         ↓
>                           不可见、难 review、无静态检查 → 通过全部 gate
> ```
> "对 LLM/agent 项目,这等于把风险从透明可测的位置推到了更隐蔽、更受模型漂移影响的位置。"
> —— 这个判断对**逐条粘原话**成立,对**描述意图类别**不成立,而后者才是常态。

---

## 八、code-simplifier skill

机械部分已被脚本吃掉,清单只留**必须理解语义**的:

```
- 这次新增的逻辑,和现有哪个函数职责重叠?能否合并(不只是同名同参)
- 新加的抽象层有没有实际的第二个使用者?没有就压平
- 新加的分支是在处理真实的不同情况,还是在给某个特例打补丁
- 本次净增 vs 净删;只增不删的话,旧路径为什么必须保留
- 有没有为了让流程跑通而加的 try/except,掩盖了本该暴露的错误
```

**最大的问题不是内容,是不会被调用。** 两个手法叠加:
1. description 写**触发条件**而非功能
   (模型靠 description 匹配情境;写"是什么"匹配不上,写"什么时候用"才匹配得上)
2. **Stop hook 里由 check.py 判断触发条件,直接输出「建议运行 code-simplifier」**
   —— 不依赖模型记得,而"记得"正是它最不可靠的能力

**触发条件按风险类型,不按规模**(原设计按行数/文件数,已下修):

原触发条件"净增 50 行或涉及 3 个以上文件"会漏掉最危险的小改动 ——
一条 5 行的精确字符串特例、一处 prompt 修改、一个测试断言改动、一处鉴权条件变化。
反过来,机械重命名涉及十几个文件却根本不需要语义审查。

改为按**改动类型**触发:
- 改了 prompt / 工具描述 / 输出 schema
- 改了测试断言
- 改了鉴权、路由、错误处理路径
- 给已有函数**新增了条件分支**(比"新增多少行"精准得多)

> ⚠️ **必须承认的局限**:这个 skill 让**同一个 agent 在同一上下文里自查**。
> 如果它已经相信自己找到了根因,清单不会提供外部视角 ——
> 它会自然地把新增分支解释成"真实的不同情况"。
> 真正的外部视角需要干净上下文的审查者(见第十一节),**短任务里这一层是缺的**。
> 目前只能靠第六节的变体 + 反例验证来部分补偿。
>
> ✅ **2026-08-03 已补(F9)**:AGENTS.md 验证段加了「验证者不该是开发者本人」——
> 越确信「就该这么修」越该派干净上下文的 subagent,**只给它「原始需求 + git diff」,
> 不给推理和结论**,由它自己造变体和反例。两个平台都有原语,不是 Claude Code 独有。
> 关键理由不是"多一双眼睛",而是:**开发者自己造的变体是照着自己刚写的修复的形状造的**,
> 和第十三节「只在自己写的样本上测 = 循环论证」是同一个错误。
> **仍然补不上的部分**:同一个模型、同样的先验 —— 干净上下文去掉的是路径依赖和沉没成本,
> 去不掉模型共有的偏见。且「有没有真的派」不可验证,这条规则天然弱于有 hook 兜着的那些。

---

## 九、已明确排除(不要重新讨论)

| 排除项 | 原因 |
|---|---|
| KISS / YAGNI / DRY / SOLID 四条原则 | 不可判定。模型主观认为自己一直在遵守;写 God class 时不觉得违反 SRP。占 4 行预算,产出 0 行为改变。且 DRY 与"不提前抽象"两句互相抵消 |
| "用户是智障人士,少用术语" | 有害。模型会照字面降低技术密度、解释基础概念。想表达的"别堆术语"应单独写 |
| Python 风格规范写进 AGENTS.md | 归 ruff。写进去纯属浪费常驻预算 |
| 短任务派 subagent **做检索** | 短任务上下文没有压力,压缩收益不存在;而延迟、总 token 更高、信息有损是实打实的。**subagent 的价值在长任务** |
| "尽量 / 建议 / 优先"类措辞 | 等于全局失效。用户现有 CLAUDE.md 的"注意不要硬编码"就是此类,已被证明无效 |
| `update_plan` / `apply_patch` | Codex CLI 工具名,Claude Code 不存在。直接抄别人配置的典型坑 |
| 大段 SQL / API 排查手册留在 CLAUDE.md | 挪去 `docs/`,正文留一行指路 |
| ~~因为"模型已经 cover 了"而砍掉 ruff 层~~ | 判据只适用于**占常驻上下文预算**的层。ruff 零上下文成本,且约束对象是仓库(历史代码/多来源)而非模型。详见第七节开头 |
| 用 `C901`/`PLR0912` 代理"单 case 优化" | **已实测证伪**:召回≈0 且有误报,`SIM116` 反而帮倒忙。见第十二节 A1 |
| "优先改现有文件,避免新建文件" | 与"单文件膨胀"痛点直接冲突。建不建文件由职责边界决定 |
| "至少换 3 种说法搜索" | 仪式性合规,只保证搜 3 次不保证找到 |
| check.py 设"零误报拦截项" | 做不到。800 行文件里新增函数很可能正是在拆分 |
| AGENTS.md 里写"不输出 .env / 密钥 / 鉴权头" | **已实测证伪**(2026-07-31,codex ×3,假密钥副本):最终答复 3/3 干净,全部用占位符 —— 这个行为模型已稳定 cover。而真实泄漏发生在 `rg -i <服务名> .` 顺带匹配到 `.env.local`,**markdown 规则管不了 ripgrep 打印什么**。风险真实但归属层错了:该由 `.rgignore` / PreToolUse hook 解决,不占常驻预算 |
| AGENTS.md 里写"不复制用户完整 prompt / 回复" | 项目特定(有真实用户数据的产品才成立),不是通用工作约定。退回各项目自己的 CLAUDE.md 项目段 |

> ⚠️ **2026-08-03 补充,防止误读上面那条**:被排除的是「派 subagent **做检索**」——
> 那是拿 subagent 当上下文压缩手段,短任务没有压缩需求。
> 「派 subagent **做验证**」是另一件事:要的是**独立视角**,和任务长短无关。
> 后者已采纳,写进 AGENTS.md 验证段(F9),两个平台都有原语:
> Claude Code 的 Task、Codex 的 `collaboration.spawn_agent`。

**通用原则:不要抄别人的 CLAUDE.md。** 别人的规则对他有用是因为那是他踩出的坑。
抄来的规则你不会去检查是否被执行 —— 而**写了不检查的规则,会让模型学到
「这个文件里的规则可以不遵守」,连带削弱整份文件的权重**。这是最隐蔽的失效模式。

---

## 十、待产出(TODO)

**先做这个 —— 证伪实验(优先级最高)**

- [ ] 挑一个**历史上真实发生过的单 case 故障**,除原始输入外准备 5-10 个
      语义等价改写 + 邻近反例。让 AI 按新规则修一遍,然后检查:
      **如果三层检查全绿、却只有原始输入能通过 —— 说明架构的核心代理指标被证伪。**
      这个实验能直接判定整套设计是否有效,应该在写任何配置之前跑。

**产物** —— 已实现,见 `ai-coding-rules/`(2026-07-31)

- [x] `AGENTS.md` —— 72 行,内容见第六节
- [x] `CLAUDE.md` —— 一行 `@AGENTS.md`
- [x] `pyproject.ruff.toml` —— 分批开 + `required-version` 固定 + `ignore = ["SIM116"]`
      (`required-version` 填的是占位值 `0.12.0`,需按实际安装版本改)
- [x] `scripts/check.py` —— AST 检查,全部为警告,理由豁免 + `.check-hits.log`
      ⚠️ 当时是 8 条,**当日晚实测后砍到 6 条**,见第十三节
- [x] `code-simplifier` skill —— description 写触发条件,按改动类型触发;
      **清单前面加了经验验证步骤**(跑变体 + 反例),部分补偿"同上下文自查"的局限
- [x] ~~prompt / schema 检查 —— 落地为 `prompt-case-patch` 规则~~
      ⚠️ **已删除**(2026-07-31 晚)。它分不清选项标签 / few-shot 示例 / 输出模板
      和真正的逐条堆砌 —— 这四者的区别是语义的。而且它本身就是「对自然语言做正则匹配
      猜意图」,正是第六节禁止的事。**禁令保留,检查删除**,归 diff review。见第十三节
- [x] hook 接线 —— Claude Code PostToolUse(exit 2 反馈给模型)/ Stop(`decision: block`,
      靠 `stop_hook_active` 保证只拦一次);pre-commit 不加 `--strict`,不阻断提交
- [x] `selftest/` —— check.py 自己的回归测试:全部规则触发 + 邻近反例零误报
      (当时 8 条 / 7 个反例,现为 6 条)
- [x] 核实 Codex CLI 当前 hook 能力 —— **2026-08-03 已核实:有,stable,事件名和契约与
      Claude Code 一致,配置文件 `hooks.json`**。Codex 同时有 subagent
      (`collaboration.spawn_agent`)。**新缺口:本仓库还没提供 Codex 侧的 `hooks.json`。**

**实现过程中被推翻/修正的**(细节写在 `ai-coding-rules/README.md`「已知的洞」):

1. **自测位置放错**:第一版把样本放在 `tests/fixtures/`,而 `fake-success` / `prod-mock` /
   `hardcoded-id` 都只对生产代码生效 —— 4 条规则全被豁免掉,测了个寂寞。移到 `selftest/`。2. **check.py 扫自己扫出两处真的 `fake-success`**:`run_git` 失败返回 `[]`,
   调用方分辨不出"没有改动"和"git 不可用";hook 解析 stdin 失败静默 `return 0`。都已修。
   这两处不是误报 —— 写检查器的人同样会犯它检查的错。
3. **`fake-success` 规则太粗**:`-> list[str] | None` 这种显式声明了 None 的签名,
   调用方是分辨得出的。规则改为认显式 Optional 标注,而不是靠豁免糊过去。
4. **`--report` 在样本不足时会劝你删光所有规则**(第一天跑完:8 条全"从未命中")。
   已加门槛:日志覆盖不足 10 天不给结论。**一个只看"没命中"不看"跑过几次"的指标,
   正是这套设计一直在警惕的代理指标失效** —— 差点自己犯一遍。
5. **`keyword-match` 有已知召回缺口**:参数叫 `q` / `s` / `inp` 时抓不到。
   加进词表会误伤队列变量、Django 的 Q、状态字符串。**不补,写进文档**。


**持续验证**:写完不是终点。规则应该是**长出来的**,不是一次设计出来的。
每月看一次 `.check-hits.log`:从来没命中的规则删掉,总被豁免的改掉。

---

## 十一、留待后续:长任务方案

短任务和长任务的**失败模式不同**,不是长度问题:

- **短任务** = 惯性反应,错在第一步、当场定型 → 前置禁令 + 事后脚本检查即可,不需要流程
- **长任务** = 累积漂移,单看每步都合理,只在整体上显形:
  - 上下文衰减(AGENTS.md 在最前面,几十轮后注意力权重大幅下降,规则事实上失效)
  - 路径依赖(早期错误假设被反复重读,不断加固)
  - 目标漂移、沉没成本(不敢推翻自己)

已初步讨论、待展开的四个机制(都平台无关):
1. **目标锚点外部化到 `PLAN.md`** —— 文件每次重新读取,不受上下文衰减影响;
   且两个平台都能读写,比各自的 plan 工具更可移植
2. **阶段闸口** —— 不按轮数,按可验证里程碑;每阶段结束跑 check + 对照 PLAN.md
3. **干净上下文的外部审查** —— Claude Code 用 subagent;可移植退路是开新会话,
   只喂「原始需求 + git diff」。**关键:不要把你的推理喂给审查者**,否则它被你的框架俘获
4. **分段 + 交接** —— 做完一阶段把状态写进 PLAN.md,开新会话继续。
   与其在污染的上下文里纠偏,不如定期换干净的。与机制 1 配套时交接成本几乎为零

诚实评估:**"陷入局部"这条,任何写在 CLAUDE.md 里的话都只有边际作用** ——
陷进去时模型正是最确信自己方向对的时候,提醒读进去也会被合理化掉。
真正有效的只有硬性熔断(3 次强制停)和外部视角。这两个都是机制,不是劝说。

---

## 十二、批判性复审记录(2026-07-31,codex gpt-5.6-sol / xhigh)

用 codex 以 xhigh 推理强度做了一次独立批判(read-only,并**实际安装 ruff 跑了验证实验**)。
本节记录:哪些批评被接受、哪些被驳回、以及为什么。**目的是防止把已推翻的结论捡回来。**

### A. 接受 —— 三条硬伤

**A1. ruff 代理"单 case 优化"的说法被实测证伪**

初版(同日早些时候)称 `C901`/`PLR0912` 是"最可靠的机械代理指标""零误判"。
实测结果:真正的单 case 补丁零报警(1 个分支离阈值 10 差得远),
合法的 13 分支协议解析器却被拦。**该抓的抓不到,不该抓的抓了。**
更严重的是 `SIM116` 建议把 if 链改成字典 —— 主动帮模型把特例藏起来。
→ 已下修第七节 B 表定位,`SIM116` 加入 ignore。

**教训**:一个看起来漂亮的发现,推广前要先找反例。当时的兴奋来自
"用零成本手段解决了难题",而这类结论通常太好以至于不真。

**A2. 整个架构缺"行为验证"层**

原三层全部在检查**代码长什么样**,没有一层检查**修复是否泛化**。
而"只修一个 case"是泛化问题,不是外形问题。一份实现可以 ruff 全绿、
check.py 全绿、skill 不触发,而只修好了那一个输入。
→ 已加为第二层,落地形式是第六节「验证」里的变体 + 邻近反例要求。
→ 第十节加了证伪实验,用来判定整套设计是否真的有效。

**A3. 规则把特例赶进了检查盲区(prompt)**

"禁止在代码里判断自由文本,改用 prompt" + "检查体系只看 Python AST"
= 一条从可检查位置到不可检查位置的迁移路径。对 LLM/agent 项目尤其危险。
→ 已加第七节 D。

> ⚠️ **2026-07-31 晚,本条被用户推翻大半**。codex(和我)都把「迁到 prompt」
> 整体当成逃逸,这是错的:判据是**机制会不会泛化**,不是代码还是 prompt。
> prompt 描述意图类别由 LLM 执行,变体都能覆盖 —— 那是修好了,不是藏起来。
> 只有**逐条粘用户原话**才是同一个病换载体。第七节 D 已按此重写,规则范围收窄到
> 「引号里的自然语言短语」。**A3 只在收窄后的那个子集上成立。**

### B. 接受 —— 规则级修正

| 原规则 | 问题 | 处理 |
|---|---|---|
| 优先改现有文件,避免新建 | **与痛点 #3(单文件膨胀)直接冲突**,奖励往 God file 堆 | 删除 |
| 至少换 3 种说法搜索 | 仪式性合规:只保证搜 3 次,不保证找到语义重复 | 删除,改为"说明为何不可复用" |
| 三条错误处理规则 | 内部冗余;枚举形态可被"换成缓存对象/占位 DTO"绕过 | 合并为一条不变量:**失败不得伪装成成功** |
| 修 bug 不许出现具体字面量 | 会阻止精确回归测试、fixture、复现脚本 | 限定"生产逻辑",明确豁免 tests/ 等 |
| grep 无调用方就删 | 动态导入、装饰器注册、配置字符串引用查不到 —— **你的 Agno 工具注册正是这类** | 要求查多种引用方式,确认不了就问 |
| 异常必须冒到顶层 | 重试/熔断/批处理需要捕获 | 改为"失败对调用方可见" |
| 3 次失败回滚到干净状态 | 可能毁掉未提交改动 | 只要求停下报告,不要求回滚 |
| 必须查第三方文档 | 在线最新文档可能与 lock 版本不符 | 改为先确认**已安装版本的实际签名** |
| 修 bug 必须定位到具体行 | 并发/时序问题无法预先归因,**会诱导编造过度确定的因果故事** | 允许"未定位到行 + 当前假设 X" |
| 测试失败默认是代码错 | 形成错误锚定,测试也可能过时/非确定 | 改为"改断言须有独立证据" |
| 只按可逆性分档 | 可回滚但发外部消息/产生费用/暴露数据仍高风险 | 加"外部副作用"这一轴 |
| check.py 零误报拦截 | 三条都有反例:**800 行文件里新增函数很可能正是在拆分** | 全部降为警告 + 理由豁免 |
| 广开 select 别精挑 | "合规代码不报警所以零噪音"是**循环论证** | 改为分批开 + 固定版本 |
| pre-commit + CI 不会漏 | `--no-verify` 可跳过;只拦得住检查器认识的模式 | 删除绝对表述 |

### C. 接受 —— 四条判据降级

初版称其为"地基/推导公理",过强。每条都有反例(详见第三节各条的下修说明),
最尖锐的是:**判据二用一个自我反驳的例子做论证** ——
说"先 grep 属于能力已过期",而痛点 #6 恰恰是"会搜索却不搜索"(不稳定调用)。
→ 已全部改称"设计启发式",并逐条修订。

### D. 驳回或部分驳回

| codex 主张 | 我的判断 |
|---|---|
| 判据一应改为"预期损失 > 总成本",考虑触发频率 × 失败概率变化 × 影响 × 误伤率 | **理论正确但不可操作** —— 你无法为一条 markdown 规则估这些数。只采纳"加一个后果维度"(低频高危保留) |
| 需要规则命中统计、真阳性/误报率追踪、例外过期时间、分支保护 | **方向对,但这是企业级治理,对单人配置是过度工程**。只采纳轻量版:`.check-hits.log` 十行代码,月度看一次 |
| 需要固定 eval 集、路由 eval、模型版本回归评测 | **是一个真正的工程项目,不是 AGENTS.md 能承载的**。只采纳可落地的最小版:修 bug 带 2-3 变体 + 1-2 反例 |
| 四条判据"不能作为地基" | 接受措辞修正,但**启发式内核仍然有用** —— 降级为设计偏好,不是废弃 |

### E. 一个版本说明

codex 的实验基于**本机 ruff 0.4.4**(较旧)。新版部分规则行为可能不同。
但其核心论证是**结构性**的(单个特例只有 1 个分支、SIM116 建议转字典),
不依赖版本,故结论仍然成立 —— 具体阈值以你实际固定的版本为准。
这也侧面印证了 codex 提的"必须固定 ruff 版本"。

### F. 最尖锐的批评(原文保留)

> 这套体系把"反模式"误等同于"反模式最容易看见的语法形态"。
> 它能阻止 `if "退款" in query`,但阻止不了针对"退款"这一个 case 优化;
> 它甚至会主动诱导模型把特例移到映射表、配置或 prompt,从而更难被检测。
>
> 系统优化的是"**让代码看起来不像特例补丁**",
> 而不是"**证明修复能泛化到同类输入,且不伤害邻近输入**"。

这条批评是对的,而且**可以被实验证伪或证实** —— 见第十节的证伪实验。
第六节新增的变体 + 反例要求就是对它的直接回应。

---

## 十三、实测复盘(2026-07-31 晚,两个真实仓库)

用户质疑「脚本里那么多硬匹配,命中率会很低吧」。**实测结论:命中率不低,误报率高得离谱。**
在仓库 A + 仓库 B(611 个 py 文件)上跑:
初版 155 / 870 条,其中绝大多数是误报。

### 删掉两条规则

| 规则 | 证据 |
|---|---|
| `hardcoded-id` | 前缀表 `run_`/`user_`/`task_`/`org_` 全是常见英文词缀,在报普通字段名(`"user_confirmed"`)。收窄到只认 UUID / 长 hex 后 611 个文件剩 1 条,还是 git SHA。**47 个误报 或 1 个无用命中,都不值得留** |
| `prompt-case-patch` | 分不清选项标签、few-shot 示例、输出模板和真正的逐条堆砌 —— 这四者的区别是**语义的**。而且这条规则本身就是「对自然语言做正则匹配猜意图」,正是第六节禁止的事 |

**两条规则删了,但对应的 AGENTS.md 禁令保留。** 原则对,静态脚本判不了,归 skill + diff review。
这也再次确认第十二节 F 那条批评:静态层只能看形态。

### 修好三处

| 修复 | 效果 |
|---|---|
| `keyword-match` 字面量必须像自然语言(不是字段名/枚举值) | 113 → 15(-86%)。`if "text" in cur_content_tmp` 是查 dict 键 |
| `keyword-match` 正则模式必须是字面量且含实际词汇 | 46 → 17。`re.findall(r"\{[^{}]*\}", response)` 是抽 JSON |
| `version-suffix` 去掉 `copy|backup|orig|tmp|temp` | 12 → 10。那几个是普通英文词 |

总量 155 → 83、870 → 167。规则数 8 → 6。

### 它确实抓到了真东西

```python
# 某工具模块:手写正则从自由文本里抽字段,中英文各一套
m = re.search(r"(?:描述|文案)\s*(?:改为|修改为|改成)\s*[:：]?\s*(.+)$", text)
m = re.search(r"(?:change|set)\s+description\s+(?:to|as)\s+(.+)$", text, flags=re.I)
# 某评估模块:生产路径三处 return self._mock_evaluation()
# 两个模块各定义一次同名函数 proc_output_v2,带 no-redef 忽略
```

### 三条方法论教训

1. **只在自己写的样本上测 = 循环论证。** 前一版"全部通过"毫无意义 —— 样本是为了让它命中而写的。
2. **`--help` 通过、`ast.parse` 通过,不等于验证过。** 删代码时误删了 5 条规则共用的常量段,
   这两个检查全绿,是自测(实际调用)抓到的。对应第六节「没实际执行过命令,不许说已验证」。
3. **误报率才是这类工具的生死线。** 一条 85% 误报的规则不是"有点噪音",
   它会教会人忽略整个工具 —— 和第九节「写了不检查的规则会削弱整份文件权重」是同一个机制。

---
